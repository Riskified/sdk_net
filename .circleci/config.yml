version: 2.0
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk

    working_directory: ~/repo
    steps:
      - checkout
      
workflows:
  version: 2
  main:
    jobs:
      - build:
