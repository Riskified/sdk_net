version: 2

jobs:
  build: # name of your job
    machine:
      -image: circleci/windows@2.2.0/vs2019

    steps:
       - checkout
       - run: 
           shell: powershell.exe 
           command: Write-Host 'Hello, Windows'
