version: 2

jobs:
  build: # name of your job
    machine: windows-default # Windows machine image
    
    steps:
      # default shell is Powershell
      - run: systeminfo
      - run:
         command: echo hello && echo bash
         shell: bash.exe
      - run:
         command: echo hello & echo cmd
         shell: cmd.exe
