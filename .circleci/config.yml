version: 2

jobs:
  build: # name of your job
    docker:
      -image: circleci/windows@2.2.0
      
    working_directory: ~/repo
    steps:
       - checkout
       - run: echo 'Hello, Windows'
       - run:
          name: "Install project dependencies"
          command: sudo dotnet install
           
