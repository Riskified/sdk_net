version: 2

jobs:
  build: # name of your job
    docker:
      - image: mcr.microsoft.com/dotnet/sdk:5.0-focal
      
    
    steps:
       - checkout
       - run: Write-Host 'Hello, Windows'
           
