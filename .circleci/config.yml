version: 2

jobs:
  build: # name of your job
    machine:
      -image: windows/default@2.2.0 # Windows machine image
    
    steps:
      # default shell is Powershell
      - run:
         command: echo hello && echo bash
         shell: bash.exe
      - run:
         command: echo hello & echo cmd
         shell: cmd.exe
