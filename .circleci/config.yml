
version: 2

jobs:
  build:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - run: whereis dotnet
      - run: pwd
      - run: sudo apt-get update
      - run: sudo apt-get install -y nuget mono-devel mono-xbuild
      - run: sudo nuget update -self
      - run: nuget restore SDK_NET.sln
      - run: mono --version
      - run: xbuild SDK_NET.sln
    

