version: 2

jobs:
  build: # name of your job
    machine:
      image: circleci/windows@4.1
    resource_class: windows.medium
    steps:
      # Commands are run in a Windows virtual machine environment
        - checkout
        - run: Write-Host 'Hello, Windows'
